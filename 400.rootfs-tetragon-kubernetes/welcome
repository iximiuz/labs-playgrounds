Welcome to iximiuz Labs' Tetragon Playground! 🚀

This is a 3-in-1 playground to explore Tetragon in Kubernetes,
Docker, and vanilla Linux environments.

You're on the Kubernetes host with Tetragon installed via the
official Helm chart.

Get started with:

```
kubectl -n kube-system rollout status ds/tetragon --watch

kubectl -n kube-system exec -it ds/tetragon -c tetragon -- \
  tetra getevents -o compact
```

For more, see: https://tetragon.io/docs/getting-started/

